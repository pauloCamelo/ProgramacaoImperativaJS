// elimina os espaçoes que estão no Inicio e no Final de uma String;

let nomeCompleto = '  Paulo Henrique Camelo    ';
    console.log(nomeCompleto.trim()); //ele não retira os espaços do meio da String